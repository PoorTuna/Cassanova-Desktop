## v0.1.3

Packaging-only release — no application changes since 0.1.0.

### Packaging

- **Windows portable build.** A single-file `.exe` is now published next to the NSIS installer. Run it without installing.
- **Branded Windows + Linux icon.** Installers and window icons now use the Cassanova logo instead of the default Electron one.
- **Snap target removed.** electron-builder routes snap publishing to the Snap Store regardless of per-target `publish` overrides, and we don't have store credentials. Linux users get AppImage, deb, rpm, pacman, and tar.xz instead.

### Build / CI

- Linux runner installs `libarchive-tools` so the `pacman` target stops failing on a clean image.
- The icon source is pinned to `build/icon-square.png` to keep electron-builder from treating it as an `.icns` master.

### Known limitations

- **Binaries are unsigned.** Windows SmartScreen and macOS Gatekeeper will warn on first launch. Code-signing is on the roadmap but not in this release.
- **macOS icon falls back to the default.** The current logo asset is 294×298, below the 512×512 minimum for `.icns`. macOS builds still ship; they just use the stock Electron icon until a higher-resolution master is added.

### Artifacts

| Platform | File |
| --- | --- |
| Windows | `Cassanova-Desktop-Setup-0.1.3.exe` (NSIS), `Cassanova-Desktop-0.1.3-portable.exe` |
| macOS | `Cassanova-Desktop-0.1.3.dmg`, `Cassanova-Desktop-0.1.3-mac.zip` (x64 + arm64) |
| Linux | `.AppImage`, `.deb`, `.rpm`, `.pacman`, `.tar.xz` |

### Note on v0.1.1 and v0.1.2

Both were tagged but never published — each release CI run failed when snap publishing fell through to the Snap Store instead of GitHub Releases, despite per-target `publish` overrides. v0.1.3 drops the snap target outright and supersedes both.
